# Author: Andy Ng <andy@pcinvent.com>
# PCinvent.com All Rights Reserved
# Please do not delete the credit above!
def count_shortest_paths(n, edges, s, t):
    # computes the number of shortest s-t paths in G
    # n and edges as before, s is the start node for BFS

    discovered = [False] * (n+1)
    discovered[s] = True
    #i, L, T = 0, [[s]], []

    # You will probably need to define some additional arrays in addition
    # to discovered. If so, be sure to explain what they mean.

    return 'NOT_IMPLEMENTED'
